name: home
description: Home feature - landing screen and adventure launcher
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  app_state:
    path: ../../utilities/app_state
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5

dev_dependencies:
  bdd_widget_test: ^2.1.3
  build_runner: ^2.4.9
  flutter_test:
    sdk: flutter
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
